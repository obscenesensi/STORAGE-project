#include <stdio.h> //배열을 인자로 받아 홀수를 출력해주는 프로그램void oNum(int* ptr){  printf("홀수 출력: ");      //홀수를 찾기 위해 반복문 수행     for (int i = 0; i < 10; i++)          if (ptr[i] % 2 != 0) //배열값을 나눴을때 나머지가 0이 아니면 홀수               printf("%d ", ptr[i]);    } void eNum(int* ptr){  printf("짝수 출력: "); //짝수를 찾기 위해 반복문 수행 for (int i = 0; i < 10; i++)      if (ptr[i] % 2 == 0) //배열값을 나눴을때 나머지가 0이면 짝수           printf("%d ", ptr[i]);} int main(void){  int arr[10]; int input;  printf("총 10개의 숫자 입력 \n");  //10개의 숫자를 입력받기 위해 반복문을 수행 for (int i = 0; i < 10; i++){  printf("?: ");  scanf_s("%d", &input);  arr[i] = input; }      oNum(arr); //배열을 oNum에 인자로 전달     printf("\n");     eNum(arr); //배열을 eNum에 인자로 전달     printf("\n");  return 0;}
