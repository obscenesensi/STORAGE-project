/*ctrl-z가 입력될 때가지 키보드로부터 단어들을 읽고 사용자가 입력한 문자열에서 * 제일 긴 단어를 출력하는 프로그램(Report1)을 작성하시오. 단어는 공백으로 분리한다 */ import java.io.*;  public class report1{ public static void main(String args[]) {  InputStreamReader in = new InputStreamReader(System.in);  //InputStreamReader를 사용하기 위해 객체 생성    int n = 0; //변수 선언  int i;  int min = 0, max;  String temp = "";  String longdana = "";    try{ //예외처리   System.out.println("단어를 입력하세요! 종료를 원하시면 ctrl+z를 눌러주세요");   while((i=in.read()) != -1) { //-1이 입력될때까지 반복    n++; //처음 입력받자마자 count++    temp +=(char)i; //입력받은 값을 (char)형으로 String형 변수에 넣음    if(i == 32 || i==13) { // 입력받은 값이 스페이스바(32)거나 엔터(13)라면     max = n;       // 변수 max에 count값을 넣음     if(max > min || min == n) { //max값과 min값을 비교      longdana = temp;       temp = ""; //temp 초기화      min = max; //min 값에 max값을 넣음 다음 단어의 비교를 위해서      n = 0; //count 초기화     }    }   }   System.out.print("\n입력된 단어들 중에서 제일 긴 단어는 " + longdana);  }catch(IOException e) { // 예외처리   System.out.println("입력 오류!!");  } }}
