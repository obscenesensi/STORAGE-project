public class WrapperClassTest { public static void main(String[] args) {  // TODO Auto-generated method stub  int x;  x = 10;  System.out.println(x);   Integer xObj;  //xObj = new Integer(10);  xObj = new Integer("10");  System.out.println(xObj);   //toStiring 은 쓰지않아도 인식해서 출력해줌.   System.out.println(xObj.intValue());    Integer yObj = 20;  System.out.println(yObj); //자동으로 박싱을 해준다 데이터형에 입력하듯이 그냥 객체에 20을 넣어버린다.   int w = yObj;  System.out.println(w);  //데이터형 w를 박싱되어있 yObj의 내용을 언방싱해서 데이터형에서 출력되게 했다.    int y = Integer.parseInt("234");  System.out.println(y);    int z = Integer.parseInt("234",8); //8진수로 바꿔준다.  System.out.println(z);     //Integer 안에 있는 parseInt 메소드 사용. }}//지금의 x는 데이터 값을 갖는 것이다//그러나 Wrapper클래스를 통해서 //인티저 클래스로 10의 내용을 저장하는 인티저 클래스를 객체를 만들어 저장,출력하려 한다.//박싱 - 기본 타입의 값을 Wrapper 객체로 변환하는 것//언박싱 - Wrapper 객체에 들어 있는 기본 타입의 값을 빼내는 것
