#include <stdio.h>

int main(void)
{
    float input = 1;
    float sum = 0;
    int count = 0;

    while (input != 0)
    {
        printf("수를 입력하세요: ");
        scanf("%d", &input);
        sum += input;
        count++;
    }

    printf("평균은 %f입니다.\n", sum / (count - 1));
}
